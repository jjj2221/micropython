# Board configuration for STM32F103C8T6 Blue Pill

LD_FILE = boards/stm32f103c8.ld
MCU_SERIES = stm32f1
CMSIS_MCU = STM32F103C8

# 如果需要，可以添加其他选项，例如：
# MICROPY_FLOAT_IMPL = double   # 默认是双精度，可能占用较大，但可接受
# MICROPY_PY_SYS_STDFILES = 1
